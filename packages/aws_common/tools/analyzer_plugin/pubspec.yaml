name: aws_common_plugin_loader
description: This pubspec determines the version of the analyzer plugin to load.
version: 0.5.0+2

environment:
  sdk: ^3.0.0

dependencies:
  analyzer: ">=5.1.0 <5.14.0"
  analyzer_plugin: ">=0.11.0 <0.12.0"
  aws_common: ^0.5.0
  source_span: ^1.10.0

dev_dependencies:
  amplify_lints: ^2.1.0
